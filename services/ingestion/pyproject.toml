[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "parking-ingestion"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "shapely>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["parking_ingestion"]
