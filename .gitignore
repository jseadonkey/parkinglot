.env
.env.local
.env.github
.env.github.*
deploy/.env
# Local-only overrides merged into deploy/.env by scripts/render_deploy_env.py
deploy/secrets.env
# Droplet-side backups from db_url merge scripts — may contain DATABASE_URL; never commit
deploy/.env*.bak*
deploy/.env.env.bak.*
.venv
# Accidental root snapshot from export_openapi_json.py -o openapi.json
/openapi.json
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
.next/
out/
dist/
*.egg-info/
.DS_Store
*.swp
terraform.tfstate
terraform.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform/

# Filled-in operator notes (paths, IP, key location — not as sensitive as tokens; still private)
docs/OPERATOR-PRIVATE-NOTES.md
